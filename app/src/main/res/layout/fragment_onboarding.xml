<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View"/>
        <variable
            name="viewModel"
            type="ru.android.romashkaapp.boarding.BoardingViewModel" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ru.android.romashkaapp.boarding.BoardingActivity">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tv_current_page"
            android:text="01"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            android:fontFamily="@font/roboto"
            app:layout_constraintTop_toTopOf="@+id/tv_all_pages"
            app:layout_constraintEnd_toStartOf="@+id/tv_all_pages"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/tv_all_pages"
            android:text="/05"
            android:fontFamily="@font/roboto"
            android:layout_marginTop="25dp"
            android:layout_marginEnd="24dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/view_pager"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="19dp"
            android:layout_marginEnd="24dp"
            android:layout_marginStart="24dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_current_page"
            app:layout_constraintBottom_toTopOf="@+id/btn_skip"/>

        <Button
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/btn_skip"
            style="?android:attr/buttonBarButtonStyle"
            android:text="@string/skip"
            android:textSize="14sp"
            android:fontFamily="@font/roboto_medium"
            android:textColor="#8193B0"
            android:paddingStart="24dp"
            android:paddingEnd="18dp"
            android:paddingTop="31dp"
            android:paddingBottom="36dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <com.google.android.material.button.MaterialButton
            style="@style/Widget.MaterialComponents.Button.Icon"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/mb_next"
            app:icon="@drawable/ic_next"
            app:iconTint="@android:color/white"
            app:iconTintMode="src_in"
            app:iconGravity="textEnd"
            app:iconPadding="18dp"


            android:text="@string/next"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:fontFamily="@font/roboto_medium"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:paddingStart="17dp"
            android:paddingEnd="17dp"
            android:layout_marginBottom="36dp"
            android:textAllCaps="true"
            android:textAlignment="center"
            android:layout_marginTop="31dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            app:layout_constraintTop_toBottomOf="@+id/view_pager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

<!--        android:onClick="@{view->viewModel.onClick(view)}"-->
        
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>